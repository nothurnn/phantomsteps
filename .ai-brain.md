# phantomsteps — .ai-brain.md

## Status
**Reconstructed and built (2026-08-12).** All 4 classes copied in from
`_RECOVERY\decompiled\phantomsteps-neoforge-1.21.1\` unchanged: `PhantomStepsMod`,
`PhantomStepsConfig`, `PhantomStepsEvents` (+ its nested `PendingStep`
record). No API port needed — confirmed via a real import scan (not just
trusting the prior session's notes): zero `net.minecraftforge`/SRG
(`func_*`/`field_*`) tokens anywhere in the decompiled source, and zero
`hauntedcore` references — genuinely 1.21.1-native and fully independent, as
Package 2/3's notes already concluded. `gradle.properties`/the mods.toml
template were already correctly filled in by the Package 3 skeleton
(`mod_name=Someone's There` — already matches INVENTORY.md's displayName
convention note, `mod_description` already matches the jar's own toml
verbatim) — nothing needed changing there.

Built (`./gradlew build`, BUILD SUCCESSFUL, `compileJava` genuinely ran, not
cached). Verified: `jar tf` shows all 4 real `.class` files (including the
`PendingStep` record as its own nested class), grepped the extracted
classfiles for `net.minecraftforge`/SRG tokens - clean. `javap -p` confirms
real methods/fields on all three top-level classes in both
`build/libs/phantomsteps-1.0.0.jar` and the deployed
`ModBuilds\neoforge-1.21.1\phantomsteps-1.0.0.jar`; md5 of both copies
matches exactly. This is the mod's first-ever deploy to `ModBuilds\` (no
prior jar existed there to compare against/replace).

## Scope note - do not add back what the jar never had

This build is a straightforward jar-to-skeleton port, nothing more. Per
explicit instruction, the RECOVERY.md-described fuller feature set (rhythm
mimicry, direction-bias, `trigger mimicry`/`trigger extra_step` debug
commands) was deliberately **not** added - the decompiled jar never had it
(confirmed again this session, matches the prior session's
`RESTORE_NOTES.md` finding exactly, nothing new found in the source beyond
what that file already documents). If that fuller design is ever wanted, it
needs to be built fresh on top of this queue-based skeleton, not extracted
from anywhere - there is nothing further to recover here.

Also note for later: this mod currently has **no debug-trigger command
registered at all** (confirmed via import scan - no `Commands`/
`CommandDispatcher` import anywhere), which violates HorrorTricks'
family-wide mandatory convention #2 (`CLAUDE.md`: "Debug-Trigger-Command,
der alle Gates umgeht"). Left as-is this session per the explicit
instruction not to invent missing features - flagging so a future session
building the fuller mimicry/bias design doesn't miss that the debug command
is owed too, not just optional polish.

## Old status text (pre-reconstruction, kept for history)
Empty, buildable Gradle skeleton only (Package 3, disaster recovery — see
`B:\Claude\RECOVERY.md`). No source copied in yet. `./gradlew build` verified
clean (0 source files, jar produced and confirmed present).

## ★ Reconstruction note: the documented features are NOT in the surviving jar

Only one build exists (no 1.20.1 port survived, Modrinth status was "In
Prüfung, 0 Downloads" — never actually went live):
`B:\Claude\Projekte\_RECOVERY\decompiled\phantomsteps-neoforge-1.21.1\`

**This is a starting skeleton, not a feature-complete source to port.**
Confirmed by reading it directly (full detail in that folder's
`RESTORE_NOTES.md`): RECOVERY.md section 5 describes rhythm-mimicry (steps
adopt the player's own walking cadence, one extra step after stopping),
direction-bias (favoring behind the player), and debug commands
(`trigger mimicry`, `trigger extra_step`) — **none of this exists in the
jar.** What's actually there is a simpler mechanic: 2–3 footstep sounds in a
straight line at a fully random angle/distance, on a randomized interval, no
player-speed reference, no bias, no debug command registered at all.

Treat this jar's `PendingStep`/queue-based delayed-step scheduling as a
reusable skeleton (the pattern RECOVERY.md says other mods reused), but plan
to **build** the mimicry and direction-bias logic fresh, not extract it —
it isn't there to extract. Also re-check Noah's later feedback ("too subtle
to judge by ear", "hearing these fake footsteps too often") once the fuller
version exists — that feedback was presumably about a later, undocumented
iteration.

## Not yet done
- Not added to `deploy-combined-test-mods.ps1` yet (tracked at the
  `HorrorTricks/` family level, out of scope for this session).
- No debug-trigger command (see scope note above - this is a real gap
  against the family convention, not just a missing nice-to-have).
- Not tested in an actual running game - only compile/jar-content/javap
  verification was done, per this project's own rule that "compiles" is
  never the same as "works".

## Hook test note
Auto-push post-commit hook installed and verified 2026-07-30T10:25:31Z.
